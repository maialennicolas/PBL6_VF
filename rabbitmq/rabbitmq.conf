# EcoMove/PBL6 - RabbitMQ AMQP con TLS real.
# La aplicación web y SYCD se conectan por 5671 usando el certificado del servidor.
# El puerto de administración 15672 sigue en HTTP para poder inspeccionar colas en local.

listeners.tcp = none
listeners.ssl.default = 5671

ssl_options.cacertfile = /etc/rabbitmq/ssl/ca_certificate.pem
ssl_options.certfile   = /etc/rabbitmq/ssl/server_certificate.pem
ssl_options.keyfile    = /etc/rabbitmq/ssl/server_key.pem
ssl_options.verify     = verify_none
ssl_options.fail_if_no_peer_cert = false

management.tcp.port = 15672
